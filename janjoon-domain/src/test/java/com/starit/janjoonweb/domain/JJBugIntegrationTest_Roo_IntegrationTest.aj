// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJBugDataOnDemand;
import com.starit.janjoonweb.domain.JJBugIntegrationTest;
import com.starit.janjoonweb.domain.JJBugRepository;
import com.starit.janjoonweb.domain.JJBugService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJBugIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJBugIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJBugIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJBugIntegrationTest: @Transactional;
    
    @Autowired
    JJBugDataOnDemand JJBugIntegrationTest.dod;
    
    @Autowired
    JJBugService JJBugIntegrationTest.jJBugService;
    
    @Autowired
    JJBugRepository JJBugIntegrationTest.jJBugRepository;
    
    @Test
    public void JJBugIntegrationTest.testCountAllJJBugs() {
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", dod.getRandomJJBug());
        long count = jJBugService.countAllJJBugs();
        Assert.assertTrue("Counter for 'JJBug' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJBugIntegrationTest.testFindJJBug() {
        JJBug obj = dod.getRandomJJBug();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to provide an identifier", id);
        obj = jJBugService.findJJBug(id);
        Assert.assertNotNull("Find method for 'JJBug' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJBug' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJBugIntegrationTest.testFindAllJJBugs() {
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", dod.getRandomJJBug());
        long count = jJBugService.countAllJJBugs();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJBug', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJBug> result = jJBugService.findAllJJBugs();
        Assert.assertNotNull("Find all method for 'JJBug' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJBug' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJBugIntegrationTest.testFindJJBugEntries() {
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", dod.getRandomJJBug());
        long count = jJBugService.countAllJJBugs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJBug> result = jJBugService.findJJBugEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJBug' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJBug' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJBugIntegrationTest.testFlush() {
        JJBug obj = dod.getRandomJJBug();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to provide an identifier", id);
        obj = jJBugService.findJJBug(id);
        Assert.assertNotNull("Find method for 'JJBug' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJBug(obj);
        Integer currentVersion = obj.getVersion();
        jJBugRepository.flush();
        Assert.assertTrue("Version for 'JJBug' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJBugIntegrationTest.testUpdateJJBugUpdate() {
        JJBug obj = dod.getRandomJJBug();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to provide an identifier", id);
        obj = jJBugService.findJJBug(id);
        boolean modified =  dod.modifyJJBug(obj);
        Integer currentVersion = obj.getVersion();
        JJBug merged = (JJBug)jJBugService.updateJJBug(obj);
        jJBugRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJBug' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJBugIntegrationTest.testSaveJJBug() {
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", dod.getRandomJJBug());
        JJBug obj = dod.getNewTransientJJBug(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJBug' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJBug' identifier to be null", obj.getId());
        try {
            jJBugService.saveJJBug(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJBugRepository.flush();
        Assert.assertNotNull("Expected 'JJBug' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJBugIntegrationTest.testDeleteJJBug() {
        JJBug obj = dod.getRandomJJBug();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBug' failed to provide an identifier", id);
        obj = jJBugService.findJJBug(id);
        jJBugService.deleteJJBug(obj);
        jJBugRepository.flush();
        Assert.assertNull("Failed to remove 'JJBug' with identifier '" + id + "'", jJBugService.findJJBug(id));
    }
    
}
