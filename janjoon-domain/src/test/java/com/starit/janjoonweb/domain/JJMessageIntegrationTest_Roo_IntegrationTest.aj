// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJMessageDataOnDemand;
import com.starit.janjoonweb.domain.JJMessageIntegrationTest;
import com.starit.janjoonweb.domain.JJMessageRepository;
import com.starit.janjoonweb.domain.JJMessageService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJMessageIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJMessageIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJMessageIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJMessageIntegrationTest: @Transactional;
    
    @Autowired
    JJMessageDataOnDemand JJMessageIntegrationTest.dod;
    
    @Autowired
    JJMessageService JJMessageIntegrationTest.jJMessageService;
    
    @Autowired
    JJMessageRepository JJMessageIntegrationTest.jJMessageRepository;
    
    @Test
    public void JJMessageIntegrationTest.testCountAllJJMessages() {
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", dod.getRandomJJMessage());
        long count = jJMessageService.countAllJJMessages();
        Assert.assertTrue("Counter for 'JJMessage' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJMessageIntegrationTest.testFindJJMessage() {
        JJMessage obj = dod.getRandomJJMessage();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to provide an identifier", id);
        obj = jJMessageService.findJJMessage(id);
        Assert.assertNotNull("Find method for 'JJMessage' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJMessage' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJMessageIntegrationTest.testFindAllJJMessages() {
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", dod.getRandomJJMessage());
        long count = jJMessageService.countAllJJMessages();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJMessage', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJMessage> result = jJMessageService.findAllJJMessages();
        Assert.assertNotNull("Find all method for 'JJMessage' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJMessage' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJMessageIntegrationTest.testFindJJMessageEntries() {
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", dod.getRandomJJMessage());
        long count = jJMessageService.countAllJJMessages();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJMessage> result = jJMessageService.findJJMessageEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJMessage' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJMessage' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJMessageIntegrationTest.testFlush() {
        JJMessage obj = dod.getRandomJJMessage();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to provide an identifier", id);
        obj = jJMessageService.findJJMessage(id);
        Assert.assertNotNull("Find method for 'JJMessage' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJMessage(obj);
        Integer currentVersion = obj.getVersion();
        jJMessageRepository.flush();
        Assert.assertTrue("Version for 'JJMessage' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJMessageIntegrationTest.testUpdateJJMessageUpdate() {
        JJMessage obj = dod.getRandomJJMessage();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to provide an identifier", id);
        obj = jJMessageService.findJJMessage(id);
        boolean modified =  dod.modifyJJMessage(obj);
        Integer currentVersion = obj.getVersion();
        JJMessage merged = jJMessageService.updateJJMessage(obj);
        jJMessageRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJMessage' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJMessageIntegrationTest.testSaveJJMessage() {
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", dod.getRandomJJMessage());
        JJMessage obj = dod.getNewTransientJJMessage(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJMessage' identifier to be null", obj.getId());
        try {
            jJMessageService.saveJJMessage(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJMessageRepository.flush();
        Assert.assertNotNull("Expected 'JJMessage' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJMessageIntegrationTest.testDeleteJJMessage() {
        JJMessage obj = dod.getRandomJJMessage();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJMessage' failed to provide an identifier", id);
        obj = jJMessageService.findJJMessage(id);
        jJMessageService.deleteJJMessage(obj);
        jJMessageRepository.flush();
        Assert.assertNull("Failed to remove 'JJMessage' with identifier '" + id + "'", jJMessageService.findJJMessage(id));
    }
    
}
