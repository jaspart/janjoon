// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJVersionDataOnDemand;
import com.starit.janjoonweb.domain.JJVersionIntegrationTest;
import com.starit.janjoonweb.domain.JJVersionRepository;
import com.starit.janjoonweb.domain.JJVersionService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJVersionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJVersionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJVersionIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJVersionIntegrationTest: @Transactional;
    
    @Autowired
    JJVersionDataOnDemand JJVersionIntegrationTest.dod;
    
    @Autowired
    JJVersionService JJVersionIntegrationTest.jJVersionService;
    
    @Autowired
    JJVersionRepository JJVersionIntegrationTest.jJVersionRepository;
    
    @Test
    public void JJVersionIntegrationTest.testCountAllJJVersions() {
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", dod.getRandomJJVersion());
        long count = jJVersionService.countAllJJVersions();
        Assert.assertTrue("Counter for 'JJVersion' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJVersionIntegrationTest.testFindJJVersion() {
        JJVersion obj = dod.getRandomJJVersion();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to provide an identifier", id);
        obj = jJVersionService.findJJVersion(id);
        Assert.assertNotNull("Find method for 'JJVersion' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJVersion' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJVersionIntegrationTest.testFindAllJJVersions() {
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", dod.getRandomJJVersion());
        long count = jJVersionService.countAllJJVersions();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJVersion', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJVersion> result = jJVersionService.findAllJJVersions();
        Assert.assertNotNull("Find all method for 'JJVersion' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJVersion' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJVersionIntegrationTest.testFindJJVersionEntries() {
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", dod.getRandomJJVersion());
        long count = jJVersionService.countAllJJVersions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJVersion> result = jJVersionService.findJJVersionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJVersion' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJVersion' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJVersionIntegrationTest.testFlush() {
        JJVersion obj = dod.getRandomJJVersion();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to provide an identifier", id);
        obj = jJVersionService.findJJVersion(id);
        Assert.assertNotNull("Find method for 'JJVersion' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJVersion(obj);
        Integer currentVersion = obj.getVersion();
        jJVersionRepository.flush();
        Assert.assertTrue("Version for 'JJVersion' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJVersionIntegrationTest.testUpdateJJVersionUpdate() {
        JJVersion obj = dod.getRandomJJVersion();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to provide an identifier", id);
        obj = jJVersionService.findJJVersion(id);
        boolean modified =  dod.modifyJJVersion(obj);
        Integer currentVersion = obj.getVersion();
        JJVersion merged = (JJVersion)jJVersionService.updateJJVersion(obj);
        jJVersionRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJVersion' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJVersionIntegrationTest.testSaveJJVersion() {
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", dod.getRandomJJVersion());
        JJVersion obj = dod.getNewTransientJJVersion(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJVersion' identifier to be null", obj.getId());
        try {
            jJVersionService.saveJJVersion(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJVersionRepository.flush();
        Assert.assertNotNull("Expected 'JJVersion' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJVersionIntegrationTest.testDeleteJJVersion() {
        JJVersion obj = dod.getRandomJJVersion();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJVersion' failed to provide an identifier", id);
        obj = jJVersionService.findJJVersion(id);
        jJVersionService.deleteJJVersion(obj);
        jJVersionRepository.flush();
        Assert.assertNull("Failed to remove 'JJVersion' with identifier '" + id + "'", jJVersionService.findJJVersion(id));
    }
    
}
