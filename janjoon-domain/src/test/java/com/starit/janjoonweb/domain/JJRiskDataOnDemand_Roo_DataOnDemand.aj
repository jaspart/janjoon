// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJContactDataOnDemand;
import com.starit.janjoonweb.domain.JJProductDataOnDemand;
import com.starit.janjoonweb.domain.JJProjectDataOnDemand;
import com.starit.janjoonweb.domain.JJRisk;
import com.starit.janjoonweb.domain.JJRiskDataOnDemand;
import com.starit.janjoonweb.domain.JJRiskRepository;
import com.starit.janjoonweb.domain.JJRiskService;
import com.starit.janjoonweb.domain.JJStatusDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect JJRiskDataOnDemand_Roo_DataOnDemand {
    
    declare @type: JJRiskDataOnDemand: @Component;
    
    private Random JJRiskDataOnDemand.rnd = new SecureRandom();
    
    private List<JJRisk> JJRiskDataOnDemand.data;
    
    @Autowired
    JJContactDataOnDemand JJRiskDataOnDemand.jJContactDataOnDemand;
    
    @Autowired
    JJProductDataOnDemand JJRiskDataOnDemand.jJProductDataOnDemand;
    
    @Autowired
    JJProjectDataOnDemand JJRiskDataOnDemand.jJProjectDataOnDemand;
    
    @Autowired
    JJStatusDataOnDemand JJRiskDataOnDemand.jJStatusDataOnDemand;
    
    @Autowired
    JJRiskService JJRiskDataOnDemand.jJRiskService;
    
    @Autowired
    JJRiskRepository JJRiskDataOnDemand.jJRiskRepository;
    
    public JJRisk JJRiskDataOnDemand.getNewTransientJJRisk(int index) {
        JJRisk obj = new JJRisk();
        setCreationDate(obj, index);
        setDescription(obj, index);
        setEnabled(obj, index);
        setName(obj, index);
        setProbability(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void JJRiskDataOnDemand.setCreationDate(JJRisk obj, int index) {
        Date creationDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationDate(creationDate);
    }
    
    public void JJRiskDataOnDemand.setDescription(JJRisk obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void JJRiskDataOnDemand.setEnabled(JJRisk obj, int index) {
        Boolean enabled = Boolean.TRUE;
        obj.setEnabled(enabled);
    }
    
    public void JJRiskDataOnDemand.setName(JJRisk obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public void JJRiskDataOnDemand.setProbability(JJRisk obj, int index) {
        Integer probability = new Integer(index);
        if (probability < 0 || probability > 100) {
            probability = 100;
        }
        obj.setProbability(probability);
    }
    
    public void JJRiskDataOnDemand.setUpdatedDate(JJRisk obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public JJRisk JJRiskDataOnDemand.getSpecificJJRisk(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        JJRisk obj = data.get(index);
        Long id = obj.getId();
        return jJRiskService.findJJRisk(id);
    }
    
    public JJRisk JJRiskDataOnDemand.getRandomJJRisk() {
        init();
        JJRisk obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return jJRiskService.findJJRisk(id);
    }
    
    public boolean JJRiskDataOnDemand.modifyJJRisk(JJRisk obj) {
        return false;
    }
    
    public void JJRiskDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = jJRiskService.findJJRiskEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'JJRisk' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<JJRisk>();
        for (int i = 0; i < 10; i++) {
            JJRisk obj = getNewTransientJJRisk(i);
            try {
                jJRiskService.saveJJRisk(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            jJRiskRepository.flush();
            data.add(obj);
        }
    }
    
}
