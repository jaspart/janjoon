// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJWorflowDataOnDemand;
import com.starit.janjoonweb.domain.JJWorflowIntegrationTest;
import com.starit.janjoonweb.domain.JJWorflowService;
import com.starit.janjoonweb.domain.JJWorkflowRepository;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJWorflowIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJWorflowIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJWorflowIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJWorflowIntegrationTest: @Transactional;
    
    @Autowired
    JJWorflowDataOnDemand JJWorflowIntegrationTest.dod;
    
    @Autowired
    JJWorflowService JJWorflowIntegrationTest.jJWorflowService;
    
    @Autowired
    JJWorkflowRepository JJWorflowIntegrationTest.jJWorkflowRepository;
    
    @Test
    public void JJWorflowIntegrationTest.testCountAllJJWorflows() {
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", dod.getRandomJJWorflow());
        long count = jJWorflowService.countAllJJWorflows();
        Assert.assertTrue("Counter for 'JJWorflow' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJWorflowIntegrationTest.testFindJJWorflow() {
        JJWorflow obj = dod.getRandomJJWorflow();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to provide an identifier", id);
        obj = jJWorflowService.findJJWorflow(id);
        Assert.assertNotNull("Find method for 'JJWorflow' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJWorflow' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJWorflowIntegrationTest.testFindAllJJWorflows() {
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", dod.getRandomJJWorflow());
        long count = jJWorflowService.countAllJJWorflows();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJWorflow', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJWorflow> result = jJWorflowService.findAllJJWorflows();
        Assert.assertNotNull("Find all method for 'JJWorflow' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJWorflow' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJWorflowIntegrationTest.testFindJJWorflowEntries() {
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", dod.getRandomJJWorflow());
        long count = jJWorflowService.countAllJJWorflows();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJWorflow> result = jJWorflowService.findJJWorflowEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJWorflow' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJWorflow' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJWorflowIntegrationTest.testFlush() {
        JJWorflow obj = dod.getRandomJJWorflow();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to provide an identifier", id);
        obj = jJWorflowService.findJJWorflow(id);
        Assert.assertNotNull("Find method for 'JJWorflow' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJWorflow(obj);
        Integer currentVersion = obj.getVersion();
        jJWorkflowRepository.flush();
        Assert.assertTrue("Version for 'JJWorflow' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJWorflowIntegrationTest.testUpdateJJWorflowUpdate() {
        JJWorflow obj = dod.getRandomJJWorflow();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to provide an identifier", id);
        obj = jJWorflowService.findJJWorflow(id);
        boolean modified =  dod.modifyJJWorflow(obj);
        Integer currentVersion = obj.getVersion();
        JJWorflow merged = jJWorflowService.updateJJWorflow(obj);
        jJWorkflowRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJWorflow' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJWorflowIntegrationTest.testSaveJJWorflow() {
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", dod.getRandomJJWorflow());
        JJWorflow obj = dod.getNewTransientJJWorflow(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJWorflow' identifier to be null", obj.getId());
        try {
            jJWorflowService.saveJJWorflow(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJWorkflowRepository.flush();
        Assert.assertNotNull("Expected 'JJWorflow' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJWorflowIntegrationTest.testDeleteJJWorflow() {
        JJWorflow obj = dod.getRandomJJWorflow();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJWorflow' failed to provide an identifier", id);
        obj = jJWorflowService.findJJWorflow(id);
        jJWorflowService.deleteJJWorflow(obj);
        jJWorkflowRepository.flush();
        Assert.assertNull("Failed to remove 'JJWorflow' with identifier '" + id + "'", jJWorflowService.findJJWorflow(id));
    }
    
}
