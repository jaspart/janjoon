// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJFlowStepDataOnDemand;
import com.starit.janjoonweb.domain.JJFlowStepIntegrationTest;
import com.starit.janjoonweb.domain.JJFlowStepRepository;
import com.starit.janjoonweb.domain.JJFlowStepService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJFlowStepIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJFlowStepIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJFlowStepIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJFlowStepIntegrationTest: @Transactional;
    
    @Autowired
    JJFlowStepDataOnDemand JJFlowStepIntegrationTest.dod;
    
    @Autowired
    JJFlowStepService JJFlowStepIntegrationTest.jJFlowStepService;
    
    @Autowired
    JJFlowStepRepository JJFlowStepIntegrationTest.jJFlowStepRepository;
    
    @Test
    public void JJFlowStepIntegrationTest.testCountAllJJFlowSteps() {
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", dod.getRandomJJFlowStep());
        long count = jJFlowStepService.countAllJJFlowSteps();
        Assert.assertTrue("Counter for 'JJFlowStep' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testFindJJFlowStep() {
        JJFlowStep obj = dod.getRandomJJFlowStep();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to provide an identifier", id);
        obj = jJFlowStepService.findJJFlowStep(id);
        Assert.assertNotNull("Find method for 'JJFlowStep' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJFlowStep' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testFindAllJJFlowSteps() {
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", dod.getRandomJJFlowStep());
        long count = jJFlowStepService.countAllJJFlowSteps();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJFlowStep', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJFlowStep> result = jJFlowStepService.findAllJJFlowSteps();
        Assert.assertNotNull("Find all method for 'JJFlowStep' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJFlowStep' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testFindJJFlowStepEntries() {
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", dod.getRandomJJFlowStep());
        long count = jJFlowStepService.countAllJJFlowSteps();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJFlowStep> result = jJFlowStepService.findJJFlowStepEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJFlowStep' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJFlowStep' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testFlush() {
        JJFlowStep obj = dod.getRandomJJFlowStep();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to provide an identifier", id);
        obj = jJFlowStepService.findJJFlowStep(id);
        Assert.assertNotNull("Find method for 'JJFlowStep' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJFlowStep(obj);
        Integer currentVersion = obj.getVersion();
        jJFlowStepRepository.flush();
        Assert.assertTrue("Version for 'JJFlowStep' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testUpdateJJFlowStepUpdate() {
        JJFlowStep obj = dod.getRandomJJFlowStep();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to provide an identifier", id);
        obj = jJFlowStepService.findJJFlowStep(id);
        boolean modified =  dod.modifyJJFlowStep(obj);
        Integer currentVersion = obj.getVersion();
        JJFlowStep merged = jJFlowStepService.updateJJFlowStep(obj);
        jJFlowStepRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJFlowStep' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testSaveJJFlowStep() {
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", dod.getRandomJJFlowStep());
        JJFlowStep obj = dod.getNewTransientJJFlowStep(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJFlowStep' identifier to be null", obj.getId());
        try {
            jJFlowStepService.saveJJFlowStep(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJFlowStepRepository.flush();
        Assert.assertNotNull("Expected 'JJFlowStep' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJFlowStepIntegrationTest.testDeleteJJFlowStep() {
        JJFlowStep obj = dod.getRandomJJFlowStep();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJFlowStep' failed to provide an identifier", id);
        obj = jJFlowStepService.findJJFlowStep(id);
        jJFlowStepService.deleteJJFlowStep(obj);
        jJFlowStepRepository.flush();
        Assert.assertNull("Failed to remove 'JJFlowStep' with identifier '" + id + "'", jJFlowStepService.findJJFlowStep(id));
    }
    
}
