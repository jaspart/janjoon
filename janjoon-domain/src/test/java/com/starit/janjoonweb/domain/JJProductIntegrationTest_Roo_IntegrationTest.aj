// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJProductDataOnDemand;
import com.starit.janjoonweb.domain.JJProductIntegrationTest;
import com.starit.janjoonweb.domain.JJProductRepository;
import com.starit.janjoonweb.domain.JJProductService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJProductIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJProductIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJProductIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJProductIntegrationTest: @Transactional;
    
    @Autowired
    JJProductDataOnDemand JJProductIntegrationTest.dod;
    
    @Autowired
    JJProductService JJProductIntegrationTest.jJProductService;
    
    @Autowired
    JJProductRepository JJProductIntegrationTest.jJProductRepository;
    
    @Test
    public void JJProductIntegrationTest.testCountAllJJProducts() {
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", dod.getRandomJJProduct());
        long count = jJProductService.countAllJJProducts();
        Assert.assertTrue("Counter for 'JJProduct' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJProductIntegrationTest.testFindJJProduct() {
        JJProduct obj = dod.getRandomJJProduct();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to provide an identifier", id);
        obj = jJProductService.findJJProduct(id);
        Assert.assertNotNull("Find method for 'JJProduct' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJProduct' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJProductIntegrationTest.testFindAllJJProducts() {
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", dod.getRandomJJProduct());
        long count = jJProductService.countAllJJProducts();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJProduct', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJProduct> result = jJProductService.findAllJJProducts();
        Assert.assertNotNull("Find all method for 'JJProduct' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJProduct' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJProductIntegrationTest.testFindJJProductEntries() {
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", dod.getRandomJJProduct());
        long count = jJProductService.countAllJJProducts();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJProduct> result = jJProductService.findJJProductEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJProduct' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJProduct' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJProductIntegrationTest.testFlush() {
        JJProduct obj = dod.getRandomJJProduct();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to provide an identifier", id);
        obj = jJProductService.findJJProduct(id);
        Assert.assertNotNull("Find method for 'JJProduct' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJProduct(obj);
        Integer currentVersion = obj.getVersion();
        jJProductRepository.flush();
        Assert.assertTrue("Version for 'JJProduct' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJProductIntegrationTest.testUpdateJJProductUpdate() {
        JJProduct obj = dod.getRandomJJProduct();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to provide an identifier", id);
        obj = jJProductService.findJJProduct(id);
        boolean modified =  dod.modifyJJProduct(obj);
        Integer currentVersion = obj.getVersion();
        JJProduct merged = (JJProduct)jJProductService.updateJJProduct(obj);
        jJProductRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJProduct' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJProductIntegrationTest.testSaveJJProduct() {
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", dod.getRandomJJProduct());
        JJProduct obj = dod.getNewTransientJJProduct(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJProduct' identifier to be null", obj.getId());
        try {
            jJProductService.saveJJProduct(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJProductRepository.flush();
        Assert.assertNotNull("Expected 'JJProduct' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJProductIntegrationTest.testDeleteJJProduct() {
        JJProduct obj = dod.getRandomJJProduct();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJProduct' failed to provide an identifier", id);
        obj = jJProductService.findJJProduct(id);
        jJProductService.deleteJJProduct(obj);
        jJProductRepository.flush();
        Assert.assertNull("Failed to remove 'JJProduct' with identifier '" + id + "'", jJProductService.findJJProduct(id));
    }
    
}
