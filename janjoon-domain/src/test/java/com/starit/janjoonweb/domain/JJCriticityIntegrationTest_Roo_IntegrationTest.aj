// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJCriticityDataOnDemand;
import com.starit.janjoonweb.domain.JJCriticityIntegrationTest;
import com.starit.janjoonweb.domain.JJCriticityRepository;
import com.starit.janjoonweb.domain.JJCriticityService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJCriticityIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJCriticityIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJCriticityIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJCriticityIntegrationTest: @Transactional;
    
    @Autowired
    JJCriticityDataOnDemand JJCriticityIntegrationTest.dod;
    
    @Autowired
    JJCriticityService JJCriticityIntegrationTest.jJCriticityService;
    
    @Autowired
    JJCriticityRepository JJCriticityIntegrationTest.jJCriticityRepository;
    
    @Test
    public void JJCriticityIntegrationTest.testCountAllJJCriticitys() {
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", dod.getRandomJJCriticity());
        long count = jJCriticityService.countAllJJCriticitys();
        Assert.assertTrue("Counter for 'JJCriticity' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJCriticityIntegrationTest.testFindJJCriticity() {
        JJCriticity obj = dod.getRandomJJCriticity();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to provide an identifier", id);
        obj = jJCriticityService.findJJCriticity(id);
        Assert.assertNotNull("Find method for 'JJCriticity' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJCriticity' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJCriticityIntegrationTest.testFindAllJJCriticitys() {
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", dod.getRandomJJCriticity());
        long count = jJCriticityService.countAllJJCriticitys();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJCriticity', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJCriticity> result = jJCriticityService.findAllJJCriticitys();
        Assert.assertNotNull("Find all method for 'JJCriticity' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJCriticity' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJCriticityIntegrationTest.testFindJJCriticityEntries() {
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", dod.getRandomJJCriticity());
        long count = jJCriticityService.countAllJJCriticitys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJCriticity> result = jJCriticityService.findJJCriticityEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJCriticity' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJCriticity' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJCriticityIntegrationTest.testFlush() {
        JJCriticity obj = dod.getRandomJJCriticity();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to provide an identifier", id);
        obj = jJCriticityService.findJJCriticity(id);
        Assert.assertNotNull("Find method for 'JJCriticity' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJCriticity(obj);
        Integer currentVersion = obj.getVersion();
        jJCriticityRepository.flush();
        Assert.assertTrue("Version for 'JJCriticity' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJCriticityIntegrationTest.testUpdateJJCriticityUpdate() {
        JJCriticity obj = dod.getRandomJJCriticity();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to provide an identifier", id);
        obj = jJCriticityService.findJJCriticity(id);
        boolean modified =  dod.modifyJJCriticity(obj);
        Integer currentVersion = obj.getVersion();
        JJCriticity merged = jJCriticityService.updateJJCriticity(obj);
        jJCriticityRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJCriticity' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJCriticityIntegrationTest.testSaveJJCriticity() {
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", dod.getRandomJJCriticity());
        JJCriticity obj = dod.getNewTransientJJCriticity(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJCriticity' identifier to be null", obj.getId());
        try {
            jJCriticityService.saveJJCriticity(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJCriticityRepository.flush();
        Assert.assertNotNull("Expected 'JJCriticity' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJCriticityIntegrationTest.testDeleteJJCriticity() {
        JJCriticity obj = dod.getRandomJJCriticity();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCriticity' failed to provide an identifier", id);
        obj = jJCriticityService.findJJCriticity(id);
        jJCriticityService.deleteJJCriticity(obj);
        jJCriticityRepository.flush();
        Assert.assertNull("Failed to remove 'JJCriticity' with identifier '" + id + "'", jJCriticityService.findJJCriticity(id));
    }
    
}
