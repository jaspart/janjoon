// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJCompanyDataOnDemand;
import com.starit.janjoonweb.domain.JJCompanyIntegrationTest;
import com.starit.janjoonweb.domain.JJCompanyRepository;
import com.starit.janjoonweb.domain.JJCompanyService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJCompanyIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJCompanyIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJCompanyIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJCompanyIntegrationTest: @Transactional;
    
    @Autowired
    JJCompanyDataOnDemand JJCompanyIntegrationTest.dod;
    
    @Autowired
    JJCompanyService JJCompanyIntegrationTest.jJCompanyService;
    
    @Autowired
    JJCompanyRepository JJCompanyIntegrationTest.jJCompanyRepository;
    
    @Test
    public void JJCompanyIntegrationTest.testCountAllJJCompanys() {
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", dod.getRandomJJCompany());
        long count = jJCompanyService.countAllJJCompanys();
        Assert.assertTrue("Counter for 'JJCompany' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJCompanyIntegrationTest.testFindJJCompany() {
        JJCompany obj = dod.getRandomJJCompany();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to provide an identifier", id);
        obj = jJCompanyService.findJJCompany(id);
        Assert.assertNotNull("Find method for 'JJCompany' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJCompany' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJCompanyIntegrationTest.testFindAllJJCompanys() {
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", dod.getRandomJJCompany());
        long count = jJCompanyService.countAllJJCompanys();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJCompany', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJCompany> result = jJCompanyService.findAllJJCompanys();
        Assert.assertNotNull("Find all method for 'JJCompany' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJCompany' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJCompanyIntegrationTest.testFindJJCompanyEntries() {
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", dod.getRandomJJCompany());
        long count = jJCompanyService.countAllJJCompanys();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJCompany> result = jJCompanyService.findJJCompanyEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJCompany' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJCompany' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJCompanyIntegrationTest.testFlush() {
        JJCompany obj = dod.getRandomJJCompany();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to provide an identifier", id);
        obj = jJCompanyService.findJJCompany(id);
        Assert.assertNotNull("Find method for 'JJCompany' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJCompany(obj);
        Integer currentVersion = obj.getVersion();
        jJCompanyRepository.flush();
        Assert.assertTrue("Version for 'JJCompany' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJCompanyIntegrationTest.testUpdateJJCompanyUpdate() {
        JJCompany obj = dod.getRandomJJCompany();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to provide an identifier", id);
        obj = jJCompanyService.findJJCompany(id);
        boolean modified =  dod.modifyJJCompany(obj);
        Integer currentVersion = obj.getVersion();
        JJCompany merged = (JJCompany)jJCompanyService.updateJJCompany(obj);
        jJCompanyRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJCompany' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJCompanyIntegrationTest.testSaveJJCompany() {
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", dod.getRandomJJCompany());
        JJCompany obj = dod.getNewTransientJJCompany(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJCompany' identifier to be null", obj.getId());
        try {
            jJCompanyService.saveJJCompany(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJCompanyRepository.flush();
        Assert.assertNotNull("Expected 'JJCompany' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJCompanyIntegrationTest.testDeleteJJCompany() {
        JJCompany obj = dod.getRandomJJCompany();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJCompany' failed to provide an identifier", id);
        obj = jJCompanyService.findJJCompany(id);
        jJCompanyService.deleteJJCompany(obj);
        jJCompanyRepository.flush();
        Assert.assertNull("Failed to remove 'JJCompany' with identifier '" + id + "'", jJCompanyService.findJJCompany(id));
    }
    
}
