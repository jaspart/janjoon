// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJAuditLogDataOnDemand;
import com.starit.janjoonweb.domain.JJAuditLogIntegrationTest;
import com.starit.janjoonweb.domain.JJAuditLogRepository;
import com.starit.janjoonweb.domain.JJAuditLogService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJAuditLogIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJAuditLogIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJAuditLogIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJAuditLogIntegrationTest: @Transactional;
    
    @Autowired
    JJAuditLogDataOnDemand JJAuditLogIntegrationTest.dod;
    
    @Autowired
    JJAuditLogService JJAuditLogIntegrationTest.jJAuditLogService;
    
    @Autowired
    JJAuditLogRepository JJAuditLogIntegrationTest.jJAuditLogRepository;
    
    @Test
    public void JJAuditLogIntegrationTest.testCountAllJJAuditLogs() {
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", dod.getRandomJJAuditLog());
        long count = jJAuditLogService.countAllJJAuditLogs();
        Assert.assertTrue("Counter for 'JJAuditLog' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testFindJJAuditLog() {
        JJAuditLog obj = dod.getRandomJJAuditLog();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to provide an identifier", id);
        obj = jJAuditLogService.findJJAuditLog(id);
        Assert.assertNotNull("Find method for 'JJAuditLog' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJAuditLog' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testFindAllJJAuditLogs() {
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", dod.getRandomJJAuditLog());
        long count = jJAuditLogService.countAllJJAuditLogs();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJAuditLog', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJAuditLog> result = jJAuditLogService.findAllJJAuditLogs();
        Assert.assertNotNull("Find all method for 'JJAuditLog' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJAuditLog' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testFindJJAuditLogEntries() {
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", dod.getRandomJJAuditLog());
        long count = jJAuditLogService.countAllJJAuditLogs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJAuditLog> result = jJAuditLogService.findJJAuditLogEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJAuditLog' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJAuditLog' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testFlush() {
        JJAuditLog obj = dod.getRandomJJAuditLog();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to provide an identifier", id);
        obj = jJAuditLogService.findJJAuditLog(id);
        Assert.assertNotNull("Find method for 'JJAuditLog' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJAuditLog(obj);
        Integer currentVersion = obj.getVersion();
        jJAuditLogRepository.flush();
        Assert.assertTrue("Version for 'JJAuditLog' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testUpdateJJAuditLogUpdate() {
        JJAuditLog obj = dod.getRandomJJAuditLog();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to provide an identifier", id);
        obj = jJAuditLogService.findJJAuditLog(id);
        boolean modified =  dod.modifyJJAuditLog(obj);
        Integer currentVersion = obj.getVersion();
        JJAuditLog merged = jJAuditLogService.updateJJAuditLog(obj);
        jJAuditLogRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJAuditLog' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testSaveJJAuditLog() {
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", dod.getRandomJJAuditLog());
        JJAuditLog obj = dod.getNewTransientJJAuditLog(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJAuditLog' identifier to be null", obj.getId());
        try {
            jJAuditLogService.saveJJAuditLog(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJAuditLogRepository.flush();
        Assert.assertNotNull("Expected 'JJAuditLog' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJAuditLogIntegrationTest.testDeleteJJAuditLog() {
        JJAuditLog obj = dod.getRandomJJAuditLog();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJAuditLog' failed to provide an identifier", id);
        obj = jJAuditLogService.findJJAuditLog(id);
        jJAuditLogService.deleteJJAuditLog(obj);
        jJAuditLogRepository.flush();
        Assert.assertNull("Failed to remove 'JJAuditLog' with identifier '" + id + "'", jJAuditLogService.findJJAuditLog(id));
    }
    
}
