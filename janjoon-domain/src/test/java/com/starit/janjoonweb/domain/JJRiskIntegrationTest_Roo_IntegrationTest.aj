// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJRiskDataOnDemand;
import com.starit.janjoonweb.domain.JJRiskIntegrationTest;
import com.starit.janjoonweb.domain.JJRiskRepository;
import com.starit.janjoonweb.domain.JJRiskService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJRiskIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJRiskIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJRiskIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJRiskIntegrationTest: @Transactional;
    
    @Autowired
    JJRiskDataOnDemand JJRiskIntegrationTest.dod;
    
    @Autowired
    JJRiskService JJRiskIntegrationTest.jJRiskService;
    
    @Autowired
    JJRiskRepository JJRiskIntegrationTest.jJRiskRepository;
    
    @Test
    public void JJRiskIntegrationTest.testCountAllJJRisks() {
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", dod.getRandomJJRisk());
        long count = jJRiskService.countAllJJRisks();
        Assert.assertTrue("Counter for 'JJRisk' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJRiskIntegrationTest.testFindJJRisk() {
        JJRisk obj = dod.getRandomJJRisk();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to provide an identifier", id);
        obj = jJRiskService.findJJRisk(id);
        Assert.assertNotNull("Find method for 'JJRisk' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJRisk' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJRiskIntegrationTest.testFindAllJJRisks() {
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", dod.getRandomJJRisk());
        long count = jJRiskService.countAllJJRisks();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJRisk', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJRisk> result = jJRiskService.findAllJJRisks();
        Assert.assertNotNull("Find all method for 'JJRisk' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJRisk' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJRiskIntegrationTest.testFindJJRiskEntries() {
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", dod.getRandomJJRisk());
        long count = jJRiskService.countAllJJRisks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJRisk> result = jJRiskService.findJJRiskEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJRisk' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJRisk' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJRiskIntegrationTest.testFlush() {
        JJRisk obj = dod.getRandomJJRisk();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to provide an identifier", id);
        obj = jJRiskService.findJJRisk(id);
        Assert.assertNotNull("Find method for 'JJRisk' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJRisk(obj);
        Integer currentVersion = obj.getVersion();
        jJRiskRepository.flush();
        Assert.assertTrue("Version for 'JJRisk' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJRiskIntegrationTest.testUpdateJJRiskUpdate() {
        JJRisk obj = dod.getRandomJJRisk();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to provide an identifier", id);
        obj = jJRiskService.findJJRisk(id);
        boolean modified =  dod.modifyJJRisk(obj);
        Integer currentVersion = obj.getVersion();
        JJRisk merged = jJRiskService.updateJJRisk(obj);
        jJRiskRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJRisk' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJRiskIntegrationTest.testSaveJJRisk() {
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", dod.getRandomJJRisk());
        JJRisk obj = dod.getNewTransientJJRisk(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJRisk' identifier to be null", obj.getId());
        try {
            jJRiskService.saveJJRisk(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJRiskRepository.flush();
        Assert.assertNotNull("Expected 'JJRisk' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJRiskIntegrationTest.testDeleteJJRisk() {
        JJRisk obj = dod.getRandomJJRisk();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJRisk' failed to provide an identifier", id);
        obj = jJRiskService.findJJRisk(id);
        jJRiskService.deleteJJRisk(obj);
        jJRiskRepository.flush();
        Assert.assertNull("Failed to remove 'JJRisk' with identifier '" + id + "'", jJRiskService.findJJRisk(id));
    }
    
}
