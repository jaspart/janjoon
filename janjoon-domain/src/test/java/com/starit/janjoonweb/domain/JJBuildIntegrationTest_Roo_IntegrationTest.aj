// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJBuildDataOnDemand;
import com.starit.janjoonweb.domain.JJBuildIntegrationTest;
import com.starit.janjoonweb.domain.JJBuildRepository;
import com.starit.janjoonweb.domain.JJBuildService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJBuildIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJBuildIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJBuildIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJBuildIntegrationTest: @Transactional;
    
    @Autowired
    JJBuildDataOnDemand JJBuildIntegrationTest.dod;
    
    @Autowired
    JJBuildService JJBuildIntegrationTest.jJBuildService;
    
    @Autowired
    JJBuildRepository JJBuildIntegrationTest.jJBuildRepository;
    
    @Test
    public void JJBuildIntegrationTest.testCountAllJJBuilds() {
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", dod.getRandomJJBuild());
        long count = jJBuildService.countAllJJBuilds();
        Assert.assertTrue("Counter for 'JJBuild' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJBuildIntegrationTest.testFindJJBuild() {
        JJBuild obj = dod.getRandomJJBuild();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to provide an identifier", id);
        obj = jJBuildService.findJJBuild(id);
        Assert.assertNotNull("Find method for 'JJBuild' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJBuild' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJBuildIntegrationTest.testFindAllJJBuilds() {
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", dod.getRandomJJBuild());
        long count = jJBuildService.countAllJJBuilds();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJBuild', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJBuild> result = jJBuildService.findAllJJBuilds();
        Assert.assertNotNull("Find all method for 'JJBuild' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJBuild' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJBuildIntegrationTest.testFindJJBuildEntries() {
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", dod.getRandomJJBuild());
        long count = jJBuildService.countAllJJBuilds();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJBuild> result = jJBuildService.findJJBuildEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJBuild' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJBuild' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJBuildIntegrationTest.testFlush() {
        JJBuild obj = dod.getRandomJJBuild();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to provide an identifier", id);
        obj = jJBuildService.findJJBuild(id);
        Assert.assertNotNull("Find method for 'JJBuild' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJBuild(obj);
        Integer currentVersion = obj.getVersion();
        jJBuildRepository.flush();
        Assert.assertTrue("Version for 'JJBuild' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJBuildIntegrationTest.testUpdateJJBuildUpdate() {
        JJBuild obj = dod.getRandomJJBuild();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to provide an identifier", id);
        obj = jJBuildService.findJJBuild(id);
        boolean modified =  dod.modifyJJBuild(obj);
        Integer currentVersion = obj.getVersion();
        JJBuild merged = (JJBuild)jJBuildService.updateJJBuild(obj);
        jJBuildRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJBuild' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJBuildIntegrationTest.testSaveJJBuild() {
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", dod.getRandomJJBuild());
        JJBuild obj = dod.getNewTransientJJBuild(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJBuild' identifier to be null", obj.getId());
        try {
            jJBuildService.saveJJBuild(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJBuildRepository.flush();
        Assert.assertNotNull("Expected 'JJBuild' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJBuildIntegrationTest.testDeleteJJBuild() {
        JJBuild obj = dod.getRandomJJBuild();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJBuild' failed to provide an identifier", id);
        obj = jJBuildService.findJJBuild(id);
        jJBuildService.deleteJJBuild(obj);
        jJBuildRepository.flush();
        Assert.assertNull("Failed to remove 'JJBuild' with identifier '" + id + "'", jJBuildService.findJJBuild(id));
    }
    
}
