// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJConfigurationDataOnDemand;
import com.starit.janjoonweb.domain.JJConfigurationIntegrationTest;
import com.starit.janjoonweb.domain.JJConfigurationRepository;
import com.starit.janjoonweb.domain.JJConfigurationService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJConfigurationIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJConfigurationIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJConfigurationIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJConfigurationIntegrationTest: @Transactional;
    
    @Autowired
    JJConfigurationDataOnDemand JJConfigurationIntegrationTest.dod;
    
    @Autowired
    JJConfigurationService JJConfigurationIntegrationTest.jJConfigurationService;
    
    @Autowired
    JJConfigurationRepository JJConfigurationIntegrationTest.jJConfigurationRepository;
    
    @Test
    public void JJConfigurationIntegrationTest.testCountAllJJConfigurations() {
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", dod.getRandomJJConfiguration());
        long count = jJConfigurationService.countAllJJConfigurations();
        Assert.assertTrue("Counter for 'JJConfiguration' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testFindJJConfiguration() {
        JJConfiguration obj = dod.getRandomJJConfiguration();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to provide an identifier", id);
        obj = jJConfigurationService.findJJConfiguration(id);
        Assert.assertNotNull("Find method for 'JJConfiguration' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJConfiguration' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testFindAllJJConfigurations() {
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", dod.getRandomJJConfiguration());
        long count = jJConfigurationService.countAllJJConfigurations();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJConfiguration', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJConfiguration> result = jJConfigurationService.findAllJJConfigurations();
        Assert.assertNotNull("Find all method for 'JJConfiguration' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJConfiguration' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testFindJJConfigurationEntries() {
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", dod.getRandomJJConfiguration());
        long count = jJConfigurationService.countAllJJConfigurations();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJConfiguration> result = jJConfigurationService.findJJConfigurationEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJConfiguration' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJConfiguration' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testFlush() {
        JJConfiguration obj = dod.getRandomJJConfiguration();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to provide an identifier", id);
        obj = jJConfigurationService.findJJConfiguration(id);
        Assert.assertNotNull("Find method for 'JJConfiguration' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJConfiguration(obj);
        Integer currentVersion = obj.getVersion();
        jJConfigurationRepository.flush();
        Assert.assertTrue("Version for 'JJConfiguration' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testUpdateJJConfigurationUpdate() {
        JJConfiguration obj = dod.getRandomJJConfiguration();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to provide an identifier", id);
        obj = jJConfigurationService.findJJConfiguration(id);
        boolean modified =  dod.modifyJJConfiguration(obj);
        Integer currentVersion = obj.getVersion();
        JJConfiguration merged = (JJConfiguration)jJConfigurationService.updateJJConfiguration(obj);
        jJConfigurationRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJConfiguration' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testSaveJJConfiguration() {
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", dod.getRandomJJConfiguration());
        JJConfiguration obj = dod.getNewTransientJJConfiguration(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJConfiguration' identifier to be null", obj.getId());
        try {
            jJConfigurationService.saveJJConfiguration(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJConfigurationRepository.flush();
        Assert.assertNotNull("Expected 'JJConfiguration' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJConfigurationIntegrationTest.testDeleteJJConfiguration() {
        JJConfiguration obj = dod.getRandomJJConfiguration();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJConfiguration' failed to provide an identifier", id);
        obj = jJConfigurationService.findJJConfiguration(id);
        jJConfigurationService.deleteJJConfiguration(obj);
        jJConfigurationRepository.flush();
        Assert.assertNull("Failed to remove 'JJConfiguration' with identifier '" + id + "'", jJConfigurationService.findJJConfiguration(id));
    }
    
}
