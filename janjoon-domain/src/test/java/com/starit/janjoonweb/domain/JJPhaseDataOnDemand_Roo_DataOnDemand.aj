// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.starit.janjoonweb.domain;

import com.starit.janjoonweb.domain.JJContactDataOnDemand;
import com.starit.janjoonweb.domain.JJPhase;
import com.starit.janjoonweb.domain.JJPhaseDataOnDemand;
import com.starit.janjoonweb.domain.JJPhaseRepository;
import com.starit.janjoonweb.domain.JJPhaseService;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect JJPhaseDataOnDemand_Roo_DataOnDemand {
    
    declare @type: JJPhaseDataOnDemand: @Component;
    
    private Random JJPhaseDataOnDemand.rnd = new SecureRandom();
    
    private List<JJPhase> JJPhaseDataOnDemand.data;
    
    @Autowired
    JJContactDataOnDemand JJPhaseDataOnDemand.jJContactDataOnDemand;
    
    @Autowired
    JJPhaseService JJPhaseDataOnDemand.jJPhaseService;
    
    @Autowired
    JJPhaseRepository JJPhaseDataOnDemand.jJPhaseRepository;
    
    public JJPhase JJPhaseDataOnDemand.getNewTransientJJPhase(int index) {
        JJPhase obj = new JJPhase();
        setCreationDate(obj, index);
        setDescription(obj, index);
        setEnabled(obj, index);
        setLevelPhase(obj, index);
        setName(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void JJPhaseDataOnDemand.setCreationDate(JJPhase obj, int index) {
        Date creationDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationDate(creationDate);
    }
    
    public void JJPhaseDataOnDemand.setDescription(JJPhase obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void JJPhaseDataOnDemand.setEnabled(JJPhase obj, int index) {
        Boolean enabled = Boolean.TRUE;
        obj.setEnabled(enabled);
    }
    
    public void JJPhaseDataOnDemand.setLevelPhase(JJPhase obj, int index) {
        Integer levelPhase = new Integer(index);
        obj.setLevelPhase(levelPhase);
    }
    
    public void JJPhaseDataOnDemand.setName(JJPhase obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public void JJPhaseDataOnDemand.setUpdatedDate(JJPhase obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public JJPhase JJPhaseDataOnDemand.getSpecificJJPhase(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        JJPhase obj = data.get(index);
        Long id = obj.getId();
        return jJPhaseService.findJJPhase(id);
    }
    
    public JJPhase JJPhaseDataOnDemand.getRandomJJPhase() {
        init();
        JJPhase obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return jJPhaseService.findJJPhase(id);
    }
    
    public boolean JJPhaseDataOnDemand.modifyJJPhase(JJPhase obj) {
        return false;
    }
    
    public void JJPhaseDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = jJPhaseService.findJJPhaseEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'JJPhase' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<JJPhase>();
        for (int i = 0; i < 10; i++) {
            JJPhase obj = getNewTransientJJPhase(i);
            try {
                jJPhaseService.saveJJPhase(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            jJPhaseRepository.flush();
            data.add(obj);
        }
    }
    
}
