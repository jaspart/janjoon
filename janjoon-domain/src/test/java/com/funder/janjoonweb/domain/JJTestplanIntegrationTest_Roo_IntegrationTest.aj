// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJTestplanDataOnDemand;
import com.funder.janjoonweb.domain.JJTestplanIntegrationTest;
import com.funder.janjoonweb.domain.JJTestplanRepository;
import com.funder.janjoonweb.domain.JJTestplanService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJTestplanIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJTestplanIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJTestplanIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJTestplanIntegrationTest: @Transactional;
    
    @Autowired
    JJTestplanDataOnDemand JJTestplanIntegrationTest.dod;
    
    @Autowired
    JJTestplanService JJTestplanIntegrationTest.jJTestplanService;
    
    @Autowired
    JJTestplanRepository JJTestplanIntegrationTest.jJTestplanRepository;
    
    @Test
    public void JJTestplanIntegrationTest.testCountAllJJTestplans() {
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", dod.getRandomJJTestplan());
        long count = jJTestplanService.countAllJJTestplans();
        Assert.assertTrue("Counter for 'JJTestplan' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJTestplanIntegrationTest.testFindJJTestplan() {
        JJTestplan obj = dod.getRandomJJTestplan();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to provide an identifier", id);
        obj = jJTestplanService.findJJTestplan(id);
        Assert.assertNotNull("Find method for 'JJTestplan' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJTestplan' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJTestplanIntegrationTest.testFindAllJJTestplans() {
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", dod.getRandomJJTestplan());
        long count = jJTestplanService.countAllJJTestplans();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJTestplan', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJTestplan> result = jJTestplanService.findAllJJTestplans();
        Assert.assertNotNull("Find all method for 'JJTestplan' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJTestplan' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJTestplanIntegrationTest.testFindJJTestplanEntries() {
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", dod.getRandomJJTestplan());
        long count = jJTestplanService.countAllJJTestplans();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJTestplan> result = jJTestplanService.findJJTestplanEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJTestplan' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJTestplan' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJTestplanIntegrationTest.testFlush() {
        JJTestplan obj = dod.getRandomJJTestplan();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to provide an identifier", id);
        obj = jJTestplanService.findJJTestplan(id);
        Assert.assertNotNull("Find method for 'JJTestplan' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJTestplan(obj);
        Integer currentVersion = obj.getVersion();
        jJTestplanRepository.flush();
        Assert.assertTrue("Version for 'JJTestplan' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTestplanIntegrationTest.testUpdateJJTestplanUpdate() {
        JJTestplan obj = dod.getRandomJJTestplan();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to provide an identifier", id);
        obj = jJTestplanService.findJJTestplan(id);
        boolean modified =  dod.modifyJJTestplan(obj);
        Integer currentVersion = obj.getVersion();
        JJTestplan merged = (JJTestplan)jJTestplanService.updateJJTestplan(obj);
        jJTestplanRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJTestplan' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTestplanIntegrationTest.testSaveJJTestplan() {
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", dod.getRandomJJTestplan());
        JJTestplan obj = dod.getNewTransientJJTestplan(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJTestplan' identifier to be null", obj.getId());
        jJTestplanService.saveJJTestplan(obj);
        jJTestplanRepository.flush();
        Assert.assertNotNull("Expected 'JJTestplan' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJTestplanIntegrationTest.testDeleteJJTestplan() {
        JJTestplan obj = dod.getRandomJJTestplan();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTestplan' failed to provide an identifier", id);
        obj = jJTestplanService.findJJTestplan(id);
        jJTestplanService.deleteJJTestplan(obj);
        jJTestplanRepository.flush();
        Assert.assertNull("Failed to remove 'JJTestplan' with identifier '" + id + "'", jJTestplanService.findJJTestplan(id));
    }
    
}
