// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJTeststepexecutionDataOnDemand;
import com.funder.janjoonweb.domain.JJTeststepexecutionIntegrationTest;
import com.funder.janjoonweb.domain.JJTeststepexecutionRepository;
import com.funder.janjoonweb.domain.JJTeststepexecutionService;
import java.util.Iterator;
import java.util.List;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJTeststepexecutionIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJTeststepexecutionIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJTeststepexecutionIntegrationTest: @ContextConfiguration(locations = "classpath*:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJTeststepexecutionIntegrationTest: @Transactional;
    
    @Autowired
    JJTeststepexecutionDataOnDemand JJTeststepexecutionIntegrationTest.dod;
    
    @Autowired
    JJTeststepexecutionService JJTeststepexecutionIntegrationTest.jJTeststepexecutionService;
    
    @Autowired
    JJTeststepexecutionRepository JJTeststepexecutionIntegrationTest.jJTeststepexecutionRepository;
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testCountAllJJTeststepexecutions() {
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", dod.getRandomJJTeststepexecution());
        long count = jJTeststepexecutionService.countAllJJTeststepexecutions();
        Assert.assertTrue("Counter for 'JJTeststepexecution' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testFindJJTeststepexecution() {
        JJTeststepexecution obj = dod.getRandomJJTeststepexecution();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to provide an identifier", id);
        obj = jJTeststepexecutionService.findJJTeststepexecution(id);
        Assert.assertNotNull("Find method for 'JJTeststepexecution' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJTeststepexecution' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testFindAllJJTeststepexecutions() {
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", dod.getRandomJJTeststepexecution());
        long count = jJTeststepexecutionService.countAllJJTeststepexecutions();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJTeststepexecution', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJTeststepexecution> result = jJTeststepexecutionService.findAllJJTeststepexecutions();
        Assert.assertNotNull("Find all method for 'JJTeststepexecution' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJTeststepexecution' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testFindJJTeststepexecutionEntries() {
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", dod.getRandomJJTeststepexecution());
        long count = jJTeststepexecutionService.countAllJJTeststepexecutions();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJTeststepexecution> result = jJTeststepexecutionService.findJJTeststepexecutionEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJTeststepexecution' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJTeststepexecution' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testFlush() {
        JJTeststepexecution obj = dod.getRandomJJTeststepexecution();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to provide an identifier", id);
        obj = jJTeststepexecutionService.findJJTeststepexecution(id);
        Assert.assertNotNull("Find method for 'JJTeststepexecution' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJTeststepexecution(obj);
        Integer currentVersion = obj.getVersion();
        jJTeststepexecutionRepository.flush();
        Assert.assertTrue("Version for 'JJTeststepexecution' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testUpdateJJTeststepexecutionUpdate() {
        JJTeststepexecution obj = dod.getRandomJJTeststepexecution();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to provide an identifier", id);
        obj = jJTeststepexecutionService.findJJTeststepexecution(id);
        boolean modified =  dod.modifyJJTeststepexecution(obj);
        Integer currentVersion = obj.getVersion();
        JJTeststepexecution merged = (JJTeststepexecution)jJTeststepexecutionService.updateJJTeststepexecution(obj);
        jJTeststepexecutionRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJTeststepexecution' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testSaveJJTeststepexecution() {
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", dod.getRandomJJTeststepexecution());
        JJTeststepexecution obj = dod.getNewTransientJJTeststepexecution(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJTeststepexecution' identifier to be null", obj.getId());
        try {
            jJTeststepexecutionService.saveJJTeststepexecution(obj);
        } catch (final ConstraintViolationException e) {
            final StringBuilder msg = new StringBuilder();
            for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                final ConstraintViolation<?> cv = iter.next();
                msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
            }
            throw new IllegalStateException(msg.toString(), e);
        }
        jJTeststepexecutionRepository.flush();
        Assert.assertNotNull("Expected 'JJTeststepexecution' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJTeststepexecutionIntegrationTest.testDeleteJJTeststepexecution() {
        JJTeststepexecution obj = dod.getRandomJJTeststepexecution();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTeststepexecution' failed to provide an identifier", id);
        obj = jJTeststepexecutionService.findJJTeststepexecution(id);
        jJTeststepexecutionService.deleteJJTeststepexecution(obj);
        jJTeststepexecutionRepository.flush();
        Assert.assertNull("Failed to remove 'JJTeststepexecution' with identifier '" + id + "'", jJTeststepexecutionService.findJJTeststepexecution(id));
    }
    
}
