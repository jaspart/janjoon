// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJContactDataOnDemand;
import com.funder.janjoonweb.domain.JJContactIntegrationTest;
import com.funder.janjoonweb.domain.JJContactRepository;
import com.funder.janjoonweb.domain.JJContactService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJContactIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJContactIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJContactIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJContactIntegrationTest: @Transactional;
    
    @Autowired
    JJContactDataOnDemand JJContactIntegrationTest.dod;
    
    @Autowired
    JJContactService JJContactIntegrationTest.jJContactService;
    
    @Autowired
    JJContactRepository JJContactIntegrationTest.jJContactRepository;
    
    @Test
    public void JJContactIntegrationTest.testCountAllJJContacts() {
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", dod.getRandomJJContact());
        long count = jJContactService.countAllJJContacts();
        Assert.assertTrue("Counter for 'JJContact' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJContactIntegrationTest.testFindJJContact() {
        JJContact obj = dod.getRandomJJContact();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to provide an identifier", id);
        obj = jJContactService.findJJContact(id);
        Assert.assertNotNull("Find method for 'JJContact' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJContact' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJContactIntegrationTest.testFindAllJJContacts() {
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", dod.getRandomJJContact());
        long count = jJContactService.countAllJJContacts();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJContact', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJContact> result = jJContactService.findAllJJContacts();
        Assert.assertNotNull("Find all method for 'JJContact' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJContact' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJContactIntegrationTest.testFindJJContactEntries() {
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", dod.getRandomJJContact());
        long count = jJContactService.countAllJJContacts();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJContact> result = jJContactService.findJJContactEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJContact' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJContact' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJContactIntegrationTest.testFlush() {
        JJContact obj = dod.getRandomJJContact();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to provide an identifier", id);
        obj = jJContactService.findJJContact(id);
        Assert.assertNotNull("Find method for 'JJContact' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJContact(obj);
        Integer currentVersion = obj.getVersion();
        jJContactRepository.flush();
        Assert.assertTrue("Version for 'JJContact' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJContactIntegrationTest.testUpdateJJContactUpdate() {
        JJContact obj = dod.getRandomJJContact();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to provide an identifier", id);
        obj = jJContactService.findJJContact(id);
        boolean modified =  dod.modifyJJContact(obj);
        Integer currentVersion = obj.getVersion();
        JJContact merged = (JJContact)jJContactService.updateJJContact(obj);
        jJContactRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJContact' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJContactIntegrationTest.testSaveJJContact() {
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", dod.getRandomJJContact());
        JJContact obj = dod.getNewTransientJJContact(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJContact' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJContact' identifier to be null", obj.getId());
        jJContactService.saveJJContact(obj);
        jJContactRepository.flush();
        Assert.assertNotNull("Expected 'JJContact' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJContactIntegrationTest.testDeleteJJContact() {
        JJContact obj = dod.getRandomJJContact();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJContact' failed to provide an identifier", id);
        obj = jJContactService.findJJContact(id);
        jJContactService.deleteJJContact(obj);
        jJContactRepository.flush();
        Assert.assertNull("Failed to remove 'JJContact' with identifier '" + id + "'", jJContactService.findJJContact(id));
    }
    
}
