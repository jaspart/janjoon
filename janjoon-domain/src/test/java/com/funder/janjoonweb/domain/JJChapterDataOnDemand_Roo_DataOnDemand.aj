// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJCategoryDataOnDemand;
import com.funder.janjoonweb.domain.JJChapter;
import com.funder.janjoonweb.domain.JJChapterDataOnDemand;
import com.funder.janjoonweb.domain.JJChapterRepository;
import com.funder.janjoonweb.domain.JJChapterService;
import com.funder.janjoonweb.domain.JJContactDataOnDemand;
import com.funder.janjoonweb.domain.JJProductDataOnDemand;
import com.funder.janjoonweb.domain.JJProjectDataOnDemand;
import java.security.SecureRandom;
import java.util.ArrayList;
import java.util.Calendar;
import java.util.Date;
import java.util.GregorianCalendar;
import java.util.Iterator;
import java.util.List;
import java.util.Random;
import javax.validation.ConstraintViolation;
import javax.validation.ConstraintViolationException;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.stereotype.Component;

privileged aspect JJChapterDataOnDemand_Roo_DataOnDemand {
    
    declare @type: JJChapterDataOnDemand: @Component;
    
    private Random JJChapterDataOnDemand.rnd = new SecureRandom();
    
    private List<JJChapter> JJChapterDataOnDemand.data;
    
    @Autowired
    JJCategoryDataOnDemand JJChapterDataOnDemand.jJCategoryDataOnDemand;
    
    @Autowired
    JJContactDataOnDemand JJChapterDataOnDemand.jJContactDataOnDemand;
    
    @Autowired
    JJProductDataOnDemand JJChapterDataOnDemand.jJProductDataOnDemand;
    
    @Autowired
    JJProjectDataOnDemand JJChapterDataOnDemand.jJProjectDataOnDemand;
    
    @Autowired
    JJChapterService JJChapterDataOnDemand.jJChapterService;
    
    @Autowired
    JJChapterRepository JJChapterDataOnDemand.jJChapterRepository;
    
    public JJChapter JJChapterDataOnDemand.getNewTransientJJChapter(int index) {
        JJChapter obj = new JJChapter();
        setCreationDate(obj, index);
        setDescription(obj, index);
        setEnabled(obj, index);
        setName(obj, index);
        setOrdering(obj, index);
        setParent(obj, index);
        setUpdatedDate(obj, index);
        return obj;
    }
    
    public void JJChapterDataOnDemand.setCreationDate(JJChapter obj, int index) {
        Date creationDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setCreationDate(creationDate);
    }
    
    public void JJChapterDataOnDemand.setDescription(JJChapter obj, int index) {
        String description = "description_" + index;
        obj.setDescription(description);
    }
    
    public void JJChapterDataOnDemand.setEnabled(JJChapter obj, int index) {
        Boolean enabled = Boolean.TRUE;
        obj.setEnabled(enabled);
    }
    
    public void JJChapterDataOnDemand.setName(JJChapter obj, int index) {
        String name = "name_" + index;
        if (name.length() > 100) {
            name = name.substring(0, 100);
        }
        obj.setName(name);
    }
    
    public void JJChapterDataOnDemand.setOrdering(JJChapter obj, int index) {
        Integer ordering = new Integer(index);
        obj.setOrdering(ordering);
    }
    
    public void JJChapterDataOnDemand.setParent(JJChapter obj, int index) {
        JJChapter parent = obj;
        obj.setParent(parent);
    }
    
    public void JJChapterDataOnDemand.setUpdatedDate(JJChapter obj, int index) {
        Date updatedDate = new GregorianCalendar(Calendar.getInstance().get(Calendar.YEAR), Calendar.getInstance().get(Calendar.MONTH), Calendar.getInstance().get(Calendar.DAY_OF_MONTH), Calendar.getInstance().get(Calendar.HOUR_OF_DAY), Calendar.getInstance().get(Calendar.MINUTE), Calendar.getInstance().get(Calendar.SECOND) + new Double(Math.random() * 1000).intValue()).getTime();
        obj.setUpdatedDate(updatedDate);
    }
    
    public JJChapter JJChapterDataOnDemand.getSpecificJJChapter(int index) {
        init();
        if (index < 0) {
            index = 0;
        }
        if (index > (data.size() - 1)) {
            index = data.size() - 1;
        }
        JJChapter obj = data.get(index);
        Long id = obj.getId();
        return jJChapterService.findJJChapter(id);
    }
    
    public JJChapter JJChapterDataOnDemand.getRandomJJChapter() {
        init();
        JJChapter obj = data.get(rnd.nextInt(data.size()));
        Long id = obj.getId();
        return jJChapterService.findJJChapter(id);
    }
    
    public boolean JJChapterDataOnDemand.modifyJJChapter(JJChapter obj) {
        return false;
    }
    
    public void JJChapterDataOnDemand.init() {
        int from = 0;
        int to = 10;
        data = jJChapterService.findJJChapterEntries(from, to);
        if (data == null) {
            throw new IllegalStateException("Find entries implementation for 'JJChapter' illegally returned null");
        }
        if (!data.isEmpty()) {
            return;
        }
        
        data = new ArrayList<JJChapter>();
        for (int i = 0; i < 10; i++) {
            JJChapter obj = getNewTransientJJChapter(i);
            try {
                jJChapterService.saveJJChapter(obj);
            } catch (final ConstraintViolationException e) {
                final StringBuilder msg = new StringBuilder();
                for (Iterator<ConstraintViolation<?>> iter = e.getConstraintViolations().iterator(); iter.hasNext();) {
                    final ConstraintViolation<?> cv = iter.next();
                    msg.append("[").append(cv.getRootBean().getClass().getName()).append(".").append(cv.getPropertyPath()).append(": ").append(cv.getMessage()).append(" (invalid value = ").append(cv.getInvalidValue()).append(")").append("]");
                }
                throw new IllegalStateException(msg.toString(), e);
            }
            jJChapterRepository.flush();
            data.add(obj);
        }
    }
    
}
