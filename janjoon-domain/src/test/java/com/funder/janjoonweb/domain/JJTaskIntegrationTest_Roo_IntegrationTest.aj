// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJTaskDataOnDemand;
import com.funder.janjoonweb.domain.JJTaskIntegrationTest;
import com.funder.janjoonweb.domain.JJTaskRepository;
import com.funder.janjoonweb.domain.JJTaskService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJTaskIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJTaskIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJTaskIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJTaskIntegrationTest: @Transactional;
    
    @Autowired
    private JJTaskDataOnDemand JJTaskIntegrationTest.dod;
    
    @Autowired
    JJTaskService JJTaskIntegrationTest.jJTaskService;
    
    @Autowired
    JJTaskRepository JJTaskIntegrationTest.jJTaskRepository;
    
    @Test
    public void JJTaskIntegrationTest.testCountAllJJTasks() {
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", dod.getRandomJJTask());
        long count = jJTaskService.countAllJJTasks();
        Assert.assertTrue("Counter for 'JJTask' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJTaskIntegrationTest.testFindJJTask() {
        JJTask obj = dod.getRandomJJTask();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to provide an identifier", id);
        obj = jJTaskService.findJJTask(id);
        Assert.assertNotNull("Find method for 'JJTask' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJTask' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJTaskIntegrationTest.testFindAllJJTasks() {
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", dod.getRandomJJTask());
        long count = jJTaskService.countAllJJTasks();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJTask', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJTask> result = jJTaskService.findAllJJTasks();
        Assert.assertNotNull("Find all method for 'JJTask' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJTask' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJTaskIntegrationTest.testFindJJTaskEntries() {
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", dod.getRandomJJTask());
        long count = jJTaskService.countAllJJTasks();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJTask> result = jJTaskService.findJJTaskEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJTask' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJTask' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJTaskIntegrationTest.testFlush() {
        JJTask obj = dod.getRandomJJTask();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to provide an identifier", id);
        obj = jJTaskService.findJJTask(id);
        Assert.assertNotNull("Find method for 'JJTask' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJTask(obj);
        Integer currentVersion = obj.getVersion();
        jJTaskRepository.flush();
        Assert.assertTrue("Version for 'JJTask' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTaskIntegrationTest.testUpdateJJTaskUpdate() {
        JJTask obj = dod.getRandomJJTask();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to provide an identifier", id);
        obj = jJTaskService.findJJTask(id);
        boolean modified =  dod.modifyJJTask(obj);
        Integer currentVersion = obj.getVersion();
        JJTask merged = (JJTask)jJTaskService.updateJJTask(obj);
        jJTaskRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJTask' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJTaskIntegrationTest.testSaveJJTask() {
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", dod.getRandomJJTask());
        JJTask obj = dod.getNewTransientJJTask(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJTask' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJTask' identifier to be null", obj.getId());
        jJTaskService.saveJJTask(obj);
        jJTaskRepository.flush();
        Assert.assertNotNull("Expected 'JJTask' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJTaskIntegrationTest.testDeleteJJTask() {
        JJTask obj = dod.getRandomJJTask();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJTask' failed to provide an identifier", id);
        obj = jJTaskService.findJJTask(id);
        jJTaskService.deleteJJTask(obj);
        jJTaskRepository.flush();
        Assert.assertNull("Failed to remove 'JJTask' with identifier '" + id + "'", jJTaskService.findJJTask(id));
    }
    
}
