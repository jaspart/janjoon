// WARNING: DO NOT EDIT THIS FILE. THIS FILE IS MANAGED BY SPRING ROO.
// You may push code into the target .java compilation unit if you wish to edit any member(s).

package com.funder.janjoonweb.domain;

import com.funder.janjoonweb.domain.JJJobDataOnDemand;
import com.funder.janjoonweb.domain.JJJobIntegrationTest;
import com.funder.janjoonweb.domain.JJJobRepository;
import com.funder.janjoonweb.domain.JJJobService;
import java.util.List;
import org.junit.Assert;
import org.junit.Test;
import org.junit.runner.RunWith;
import org.springframework.beans.factory.annotation.Autowired;
import org.springframework.test.context.ContextConfiguration;
import org.springframework.test.context.junit4.SpringJUnit4ClassRunner;
import org.springframework.transaction.annotation.Transactional;

privileged aspect JJJobIntegrationTest_Roo_IntegrationTest {
    
    declare @type: JJJobIntegrationTest: @RunWith(SpringJUnit4ClassRunner.class);
    
    declare @type: JJJobIntegrationTest: @ContextConfiguration(locations = "classpath:/META-INF/spring/applicationContext*.xml");
    
    declare @type: JJJobIntegrationTest: @Transactional;
    
    @Autowired
    JJJobDataOnDemand JJJobIntegrationTest.dod;
    
    @Autowired
    JJJobService JJJobIntegrationTest.jJJobService;
    
    @Autowired
    JJJobRepository JJJobIntegrationTest.jJJobRepository;
    
    @Test
    public void JJJobIntegrationTest.testCountAllJJJobs() {
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", dod.getRandomJJJob());
        long count = jJJobService.countAllJJJobs();
        Assert.assertTrue("Counter for 'JJJob' incorrectly reported there were no entries", count > 0);
    }
    
    @Test
    public void JJJobIntegrationTest.testFindJJJob() {
        JJJob obj = dod.getRandomJJJob();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to provide an identifier", id);
        obj = jJJobService.findJJJob(id);
        Assert.assertNotNull("Find method for 'JJJob' illegally returned null for id '" + id + "'", obj);
        Assert.assertEquals("Find method for 'JJJob' returned the incorrect identifier", id, obj.getId());
    }
    
    @Test
    public void JJJobIntegrationTest.testFindAllJJJobs() {
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", dod.getRandomJJJob());
        long count = jJJobService.countAllJJJobs();
        Assert.assertTrue("Too expensive to perform a find all test for 'JJJob', as there are " + count + " entries; set the findAllMaximum to exceed this value or set findAll=false on the integration test annotation to disable the test", count < 250);
        List<JJJob> result = jJJobService.findAllJJJobs();
        Assert.assertNotNull("Find all method for 'JJJob' illegally returned null", result);
        Assert.assertTrue("Find all method for 'JJJob' failed to return any data", result.size() > 0);
    }
    
    @Test
    public void JJJobIntegrationTest.testFindJJJobEntries() {
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", dod.getRandomJJJob());
        long count = jJJobService.countAllJJJobs();
        if (count > 20) count = 20;
        int firstResult = 0;
        int maxResults = (int) count;
        List<JJJob> result = jJJobService.findJJJobEntries(firstResult, maxResults);
        Assert.assertNotNull("Find entries method for 'JJJob' illegally returned null", result);
        Assert.assertEquals("Find entries method for 'JJJob' returned an incorrect number of entries", count, result.size());
    }
    
    @Test
    public void JJJobIntegrationTest.testFlush() {
        JJJob obj = dod.getRandomJJJob();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to provide an identifier", id);
        obj = jJJobService.findJJJob(id);
        Assert.assertNotNull("Find method for 'JJJob' illegally returned null for id '" + id + "'", obj);
        boolean modified =  dod.modifyJJJob(obj);
        Integer currentVersion = obj.getVersion();
        jJJobRepository.flush();
        Assert.assertTrue("Version for 'JJJob' failed to increment on flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJJobIntegrationTest.testUpdateJJJobUpdate() {
        JJJob obj = dod.getRandomJJJob();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to provide an identifier", id);
        obj = jJJobService.findJJJob(id);
        boolean modified =  dod.modifyJJJob(obj);
        Integer currentVersion = obj.getVersion();
        JJJob merged = (JJJob)jJJobService.updateJJJob(obj);
        jJJobRepository.flush();
        Assert.assertEquals("Identifier of merged object not the same as identifier of original object", merged.getId(), id);
        Assert.assertTrue("Version for 'JJJob' failed to increment on merge and flush directive", (currentVersion != null && obj.getVersion() > currentVersion) || !modified);
    }
    
    @Test
    public void JJJobIntegrationTest.testSaveJJJob() {
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", dod.getRandomJJJob());
        JJJob obj = dod.getNewTransientJJJob(Integer.MAX_VALUE);
        Assert.assertNotNull("Data on demand for 'JJJob' failed to provide a new transient entity", obj);
        Assert.assertNull("Expected 'JJJob' identifier to be null", obj.getId());
        jJJobService.saveJJJob(obj);
        jJJobRepository.flush();
        Assert.assertNotNull("Expected 'JJJob' identifier to no longer be null", obj.getId());
    }
    
    @Test
    public void JJJobIntegrationTest.testDeleteJJJob() {
        JJJob obj = dod.getRandomJJJob();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to initialize correctly", obj);
        Long id = obj.getId();
        Assert.assertNotNull("Data on demand for 'JJJob' failed to provide an identifier", id);
        obj = jJJobService.findJJJob(id);
        jJJobService.deleteJJJob(obj);
        jJJobRepository.flush();
        Assert.assertNull("Failed to remove 'JJJob' with identifier '" + id + "'", jJJobService.findJJJob(id));
    }
    
}
